node_modules/
dist/
.env
data/*
# Except config/annotation files shipped with the code
!data/schema-annotations.json

# Dashboard runtime files (dashboard/ is part of this monorepo since 2026-04-20;
# previously a submodule of builderz-labs/mission-control — see dashboard/README.md)
dashboard/.env
dashboard/.env.local
dashboard/.data/
dashboard/.next/
dashboard/node_modules/
dashboard/tsconfig.tsbuildinfo

# Reporting agent runtime workspace (regenerated per-run; not source)
src/instances/*/workspace/

# Claude Code runtime artifacts
.claude/scheduled_tasks.lock
