/**
 * ElevarusOS Integrations
 *
 * Third-party data source libraries used by all workflow agents.
 * Each integration is self-contained and reusable across any workflow.
 *
 * Available:
 *   ringba — Call tracking revenue, paid calls, campaign performance
 *
 * Coming soon:
 *   meta   — Meta Ads spend, impressions, CPL
 *   google — Google Ads (future)
 *   qmp    — QMP/click revenue (future)
 */
export * as ringba from "./ringba";
